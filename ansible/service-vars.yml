service_ansible_dir: "services/{{ ansible_hostname }}"
service_custom_vars: "{{ service_ansible_dir }}/vars.yml"
service_custom_tasks: "{{ service_ansible_dir }}/tasks.yml"
service_custom_nginx_conf: "{{ service_ansible_dir }}/nginx.conf"
service_data_dir: "{{ services_data_dir }}/{{ ansible_hostname }}"
service_private_dir: "{{ private_dir }}/{{ ansible_hostname }}"
service_nginx_dir: "{{ service_data_dir }}/etc/nginx"
service_server_name: "{{ ansible_hostname }}.{{ domain }}"
service_container_address: "{{ ansible_hostname }}_"
