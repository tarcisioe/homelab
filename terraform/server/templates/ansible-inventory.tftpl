# This file was generated by Terraform
[all:vars]
private_dir = "../private"
tf_generated_dir = "../tf-generated"
compose_dir = "/container"
services_data_dir = "/mnt/data/services"

[proxmox]
${ proxmox.node } ansible_host=${ proxmox.ip }

[dns]
${ dns.container.hostname } ansible_host=${ dns.container.ip }

[service_containers]
%{ for service in services ~}
${ service.container.hostname } ansible_host=${ service.container.ip }
%{ endfor ~}
